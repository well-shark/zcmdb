# ZCMDB 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# ============================================
# 数据库配置
# ============================================
POSTGRES_USER=zcmdb
POSTGRES_PASSWORD=zcmdb123
POSTGRES_DB=zcmdb
POSTGRES_HOST=localhost
# Docker环境使用 "postgres"，本地开发使用 "localhost"
POSTGRES_PORT=5432

# ============================================
# JWT 配置
# ============================================
# 生产环境必须修改为强随机字符串
# 使用以下 Python 脚本生成：
# python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=your_jwt_secret_key_here_change_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
# Token 过期时间（分钟），默认 24 小时

# ============================================
# 加密配置
# ============================================
# 生产环境必须配置，用于加密敏感信息（密码、授权码等）
# 使用以下 Python 脚本生成：
# python -c "import base64; from cryptography.fernet import Fernet; print(base64.urlsafe_b64encode(Fernet.generate_key()).decode())"
ENCRYPTION_KEY=your-32-byte-encryption-key-here-base64-encoded

# ============================================
# CORS 配置
# ============================================
# 允许的前端域名，多个用逗号分隔
CORS_ORIGINS=http://localhost:80,http://localhost:5173,http://localhost:3000

# ============================================
# 环境配置
# ============================================
# development 或 production
ENVIRONMENT=development

# ============================================
# 默认管理员账号
# ============================================
# 首次启动时自动创建，生产环境建议修改默认密码
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_EMAIL=admin@example.com

# ============================================
# 前端配置（可选）
# ============================================
# 前端 API 基础 URL，用于前端开发环境
# VITE_API_BASE_URL=http://localhost:8000/api/v1

# ============================================
# 日志配置（可选）
# ============================================
# LOG_LEVEL=INFO
